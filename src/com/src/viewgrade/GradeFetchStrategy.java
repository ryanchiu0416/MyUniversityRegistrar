package com.src.viewgrade;
/*
     The GradeFetchStrategy interface defines function(s) necessary for fetching student's grade(s).

     Pattern: Strategy (interface)
 */
public interface GradeFetchStrategy {
    String fetchGrade();
}
